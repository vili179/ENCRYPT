services:
  - type: web
    name: encrypt-app
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn -w 4 -b 0.0.0.0:$PORT "web server:app"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
